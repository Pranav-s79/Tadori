# Tadori project instructions for Codex

The active product contract is the multi-language transition in
`docs/Specs/Tadori-Multilanguage-Transition.md`. The former v2.1 documents are
historical references, not scope or schema authorities. Preserve the legacy
TS/JS fixtures as regression coverage while extending—not duplicating—the
canonical graph, store, snapshot, diff, API, and visualization systems. Use the
relevant project-local skill, run the validation skill after changes, and
maintain `IMPLEMENTATION_STATUS.md`. Do not push, publish, or create a remote
without explicit instruction.
