# Execution card 08-10: Large-repo performance

> This is the executable graph node. Open the long dossier only for a linked contract detail.

## Node

- State: `review`
- Phase / risk / complexity: `8` / `medium` / `M`
- Predecessors: `08-04`
- Successors: `10-01`
- Dossier: `blueprints/08-10-large-repo-performance.md`
- Dossier size: approximately `9,492` tokens
- Full local gate budget: `1`
- Independent validation budget: `1`

## Graph rewrite

Running `tadori serve .` cold against a 150,000-LOC synthetic TypeScript
corpus reaches an interactive package-level map in under 5 seconds; the
positions rendered are byte-identical across two independent serve cycles
against the same snapshot; every zoom level enforces its fixed node/edge/label
caps; and the viz tab's browser heap stays under 500 MB at package level on
the same corpus — each budget backed by a named, runnable, CI-excluded
benchmark script plus an automated positions-identity assertion.

## Minimum read neighborhood

- This card, the current checkpoint, and direct predecessor contract exports.
- `scripts/benchmark-incremental.mts`
- `package.json`
- `tsconfig.json`
- `IMPLEMENTATION_STATUS.md`
- `benchmark-incremental.mts`
- `README.md`
- `x/y/z`
- `packages/store/src/migrations.ts:442-458`
- `packages/mcp/src/contracts.ts`
- `blueprints/00-02-ci-pipeline.md:90-96`
- `apps/viz`
- `packages/server`
- `packages/cli`
- `packages/store/src/migrations.ts:435-458`
- `blueprints/00-02-ci-pipeline.md`
- `docs/CLI_CONTRACT.md`

## Owned output neighborhood

- `scripts/lib/syntheticCorpus.mts`
- `benchmark-incremental.mts`
- `scripts/benchmark-serve-coldstart.mts`
- `syntheticCorpus.mts`
- `scripts/benchmark-serve-positions-identity.mts`
- `scripts/benchmark-serve-memory.mts`
- `apps/viz/test/perf/memory.spec.ts`
- `apps/viz/test/perf/`
- `apps/viz/src/lod/budgets.ts`
- `apps/viz/test/lod-budgets.test.ts`
- `packages/server/test/lod-response-caps.test.ts`
- `packages/server/test/`
- `IMPLEMENTATION_STATUS.md`

## Execution slices

### Slice A

- `scripts/lib/syntheticCorpus.mts`: extract/parameterize `benchmark-incremental.mts`'s file-generation logic; re-run `pnpm benchmark:incremental` unchanged to confirm the refactor is behavior-preserving (same gates pass with the same numbers, within normal machine variance). Reason: single source of corpus generation (DECISION 08-10-A). Test: existing `benchmark-incremental.mts` gates still pass.
- `apps/viz/src/lod/budgets.ts` + `apps/viz/test/lod-budgets.test.ts`: define the frozen numbers and their unit tests (client-side request capping, label-culling function). Reason: single source of the numeric budgets before any script references them. Test: requesting `limit=9999` at package level is clamped to 500 before the fetch is issued; label function returns `<= 200` labels for a synthetic 500-node input.
- `packages/server/test/lod-response-caps.test.ts`: write against 07-01's endpoint (or a documented-pending stub if 07-01 is not yet built — see §7). Reason: server-side enforcement is the authoritative cap (DECISION 08-10-D), client-side is defense in depth only. Test: oversized `limit` request still yields a capped response.
- Focused proof: run only tests that directly observe this slice’s contract edge.
- Preservation: checkpoint/commit when the slice is coherent; push the draft branch when usage risk is material.

### Slice B

- `scripts/benchmark-serve-coldstart.mts`: implement the cold-start timing script against the 150k-LOC corpus, marked CI-excluded in its own header comment (matching 00-02's precedent). Reason: the headline 08-10 budget. Test: script runs locally, asserts `< 5000 ms`, throws with a clear message on failure (mirrors `benchmark-incremental.mts`'s throw-based gate style).
- `scripts/benchmark-serve-positions-identity.mts`: implement the two-cycle positions comparison. Reason: the byte-identical-reload contract (AD-005) is otherwise untested end-to-end. Test: two cycles against the same snapshot yield zero mismatches across all three levels.
- `scripts/benchmark-serve-memory.mts` (or the Playwright-spec equivalent): implement the heap-snapshot measurement. Reason: the memory ceiling is otherwise unverified. Test: package-level load on the 150k corpus reports `< 500 MB`.
- Focused proof: run only tests that directly observe this slice’s contract edge.
- Preservation: checkpoint/commit when the slice is coherent; push the draft branch when usage risk is material.

### Slice C

- Document the regression procedure (§17) and CI-exclusion marking consistent with 00-02 (§15). Run the full existing gate (§15) to confirm zero regression to the pre-existing suite.
- Focused proof: run only tests that directly observe this slice’s contract edge.
- Preservation: checkpoint/commit when the slice is coherent; push the draft branch when usage risk is material.

## Completion proof cut

- [ ] `scripts/benchmark-serve-coldstart.mts` reports package-level interactive time `< 5000 ms` on the 150k-LOC corpus (run locally; CI-excluded).
- [ ] `scripts/benchmark-serve-positions-identity.mts` reports zero mismatches across all three LOD levels between two independent serve cycles against the same snapshot.
- [ ] `apps/viz/test/lod-budgets.test.ts` passes: package/file node caps `<= 500`, symbol node cap `<= 1000`, edge cap `<= 1000` per level, simultaneous label cap `<= 200`.
- [ ] `packages/server/test/lod-response-caps.test.ts` passes: an over-limit request never yields an over-budget response.
- [ ] `scripts/benchmark-serve-memory.mts` reports package-level browser heap `< 500 MB` on the 150k-LOC corpus.
- [ ] All three new benchmark scripts are excluded from `00-02`'s CI workflow file (verified by `git diff` showing no new script name added to the CI workflow's run list) and each carries a `machine-variance, CI-excluded` header comment.
- [ ] `scripts/lib/syntheticCorpus.mts` extraction leaves `pnpm benchmark:incremental`'s existing four gates passing unchanged.
- [ ] Full existing suite (170+ tests) and 5/5 fixtures stay green — this blueprint touches no fixture/migration surface.
- [ ] The regression procedure (§17) is documented and named; no budget in this file is silently raised as part of this blueprint's own acceptance (raising a budget requires a separate, explicit, reviewed change).
- [ ] Independent validator reports no blocker/high finding.
- [ ] One risk-selected local completion gate passes.
- [ ] Required CI passes before merge.

## Dossier sections to open on demand

- Architecture/contract section only for a disputed public or persisted edge.
- Failure/security/accessibility sections only when the diff touches those neighborhoods.
- Review history only for an unresolved blocker/high.

## Stop boundaries

- Stop for a frozen-contract contradiction, absent predecessor contract, schema/migration data-loss risk, or inability to restore focused proofs without weakening an invariant.
- Do not stop for stale line numbers, historical “does not exist” prose, or a necessary one-hop wiring file.

## Delivery

- Completed node: status delta → commit/push → PR → CI → merge → immediately select the next frontier node.
- Usage cutoff: preserve a coherent slice in a commit and draft PR; never leave the only copy uncommitted.
