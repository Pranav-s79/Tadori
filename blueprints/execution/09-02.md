# Execution card 09-02: Rename/move coalescing views

> This is the executable graph node. Open the long dossier only for a linked contract detail.

## Node

- State: `review`
- Phase / risk / complexity: `9` / `medium` / `M`
- Predecessors: `09-01`
- Successors: `12-04`
- Dossier: `blueprints/09-02-rename-move-coalescing.md`
- Dossier size: approximately `7,602` tokens
- Full local gate budget: `1`
- Independent validation budget: `1`

## Graph rewrite

`GET /api/v1/review/diff?coalesce=coalesced` returns a `CoalescedChange[]`
view computed from the same raw `ReviewDiff` edges (09-01), applying
Stage A (identity-basis match) then Stage B (body-hash/signature match)
exactly as the frozen corrections doc and fixture 04's
`expected/coalesced-diff.json` define them; every coalesced row references
the raw rows it collapses so a user can expand back to raw, and every
ambiguous or non-matching case falls back to raw display with a stated
reason — never a silently invented certain match.

## Minimum read neighborhood

- This card, the current checkpoint, and direct predecessor contract exports.
- `schemas/expected-diff.schema.json`
- `packages/fixtures/04-diff-coalescing/expected/coalesced-diff.json`
- `file:src/legacy/helper.ts`
- `file:src/helpers/helper.ts`
- `fn:task.processTask -calls-> method:formatter.formatValue/renderValue`
- `.claude/skills/tadori-indexer/SKILL.md`
- `.agents/skills/tadori-indexer/SKILL.md`
- `agent-skills/tadori-indexer/SKILL.md`
- `packages/harness/src/compare.ts:432`
- `expected-diff.schema.json`
- `compare.ts`
- `packages/harness/src/compareDiff.ts`
- `raw-diff.json`
- `coalesced-diff.json`
- `IMPLEMENTATION_STATUS.md`
- `extract.ts`

## Owned output neighborhood

- `packages/harness/src/compareDiff.ts`
- `raw-diff.json`
- `coalesced-diff.json`
- `packages/harness/src/index.ts`
- `packages/harness/src/cli-index.ts`
- `packages/harness/test/diff-comparison.test.ts`
- `packages/server/src/coalescing.ts`
- `packages/server/src/routes/reviewDiff.ts`
- `packages/server/test/coalescing.test.ts`
- `apps/viz/src/views/ReviewDiffView.tsx`
- `IMPLEMENTATION_STATUS.md`

## Execution slices

### Slice A

- `packages/server/test/coalescing.test.ts`: write failing tests for `stageAMatch` (exact basis match, unique-candidate requirement) and `stageBMatch` (body-hash-only match among Stage-A residuals, ambiguity when two residual candidates share a body hash) using synthetic `ToolNode` fixtures modeled on fixture 04's actual node shapes (file move + method rename). Implement `stageAMatch`/`stageBMatch`. Expected: green; `stageAMatch` reproduces the fixture's two Stage-A pairs on fixture-04-shaped input, `stageBMatch` reproduces the one Stage-B pair.
- Add a synthetic recursive-function test case: a function `factorial` whose body calls itself by name, renamed to `factorialImpl` with an identical algorithm but a body hash that necessarily differs (self-call site references the new name). Assert `stageBMatch` does **not** pair it (falls into `residualRemoved`/`residualAdded`), and add a comment citing `.claude/skills/tadori-indexer/SKILL.md` lines 18-20 as the reason this is correct, not a bug. Expected: green, proving the miss is preserved.
- Implement `coalesceEdges`/`buildCoalescedChanges`; test against synthetic edge sets modeled on fixture 04's eight `edgePairs`. Expected: green.
- Focused proof: run only tests that directly observe this slice’s contract edge.
- Preservation: checkpoint/commit when the slice is coherent; push the draft branch when usage risk is material.

### Slice B

- `packages/harness/src/compareDiff.ts` + `packages/harness/test/diff-comparison.test.ts`: index fixture 04's `before`/`after` into a temp store, run the real `diffSnapshotEdges`/coalescing pipeline, assert semantic equivalence (entity-key-based, not string-id-based, since fixture ids and entity keys differ per the §4 gotcha) against both `raw-diff.json` (cross-check with 09-01) and `coalesced-diff.json` (this blueprint's oracle). Expected: green — this is the un-deferred check.
- Wire `compareFixtureDiff` into `packages/harness/src/cli-index.ts`; run `pnpm fixtures:index` and confirm it now exercises and passes the diff comparison alongside the existing snapshot comparisons.
- `packages/server/src/routes/reviewDiff.ts`: replace the 501 stub with the real coalescing call. Integration test: `GET /api/v1/review/diff?coalesce=coalesced` against fixture-04-derived snapshots returns a `ReviewDiff` whose `coalesced` array matches the oracle semantically.
- Focused proof: run only tests that directly observe this slice’s contract edge.
- Preservation: checkpoint/commit when the slice is coherent; push the draft branch when usage risk is material.

### Slice C

- `apps/viz/src/views/ReviewDiffView.tsx`: add the raw/coalesced toggle and expand-to-raw UI. Manual verification against a served fixture-04 diff.
- Update `IMPLEMENTATION_STATUS.md` per §8's documentation decision.
- Full validation gate (§15).
- Focused proof: run only tests that directly observe this slice’s contract edge.
- Preservation: checkpoint/commit when the slice is coherent; push the draft branch when usage risk is material.

## Completion proof cut

- [ ] `stageAMatch`/`stageBMatch` reproduce fixture 04's exact three `nodePairs` (two Stage A, one Stage B) with matching `basis` arrays.
- [ ] The recursive-self-reference synthetic test proves the intentional Stage-B miss is preserved (function stays in raw residual, not paired).
- [ ] `coalesceEdges`/`buildCoalescedChanges` reproduce fixture 04's eight `edgePairs` and both residual-edge lists exactly (by entity key).
- [ ] `compareFixtureDiff` is wired into `pnpm fixtures:index` and passes.
- [ ] `GET /api/v1/review/diff?coalesce=coalesced` returns a real `CoalescedChange[]` (no longer 501) whose rows all carry valid `rawRowIndexes` into the raw `edges` array.
- [ ] Every coalesced row is expandable in the UI to its underlying raw rows with zero additional network fetch.
- [ ] `IMPLEMENTATION_STATUS.md` reflects the diff-artifact check as executed, not deferred.
- [ ] Full existing suite (170+ tests) and 5/5 fixtures stay green.
- [ ] Independent validator reports no blocker/high finding.
- [ ] One risk-selected local completion gate passes.
- [ ] Required CI passes before merge.

## Dossier sections to open on demand

- Architecture/contract section only for a disputed public or persisted edge.
- Failure/security/accessibility sections only when the diff touches those neighborhoods.
- Review history only for an unresolved blocker/high.

## Stop boundaries

- Stop for a frozen-contract contradiction, absent predecessor contract, schema/migration data-loss risk, or inability to restore focused proofs without weakening an invariant.
- Do not stop for stale line numbers, historical “does not exist” prose, or a necessary one-hop wiring file.

## Delivery

- Completed node: status delta → commit/push → PR → CI → merge → immediately select the next frontier node.
- Usage cutoff: preserve a coherent slice in a commit and draft PR; never leave the only copy uncommitted.
