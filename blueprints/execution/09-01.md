# Execution card 09-01: Review diff API + raw diff UI

> This is the executable graph node. Open the long dossier only for a linked contract detail.

## Node

- State: `review`
- Phase / risk / complexity: `9` / `high` / `L`
- Predecessors: `08-06`
- Successors: `09-02`, `09-03`, `09-04`, `09-05`
- Dossier: `blueprints/09-01-review-diff-api-ui.md`
- Dossier size: approximately `7,889` tokens
- Full local gate budget: `1`
- Independent validation budget: `1`

## Graph rewrite

`GET /api/v1/review/diff` returns a `ReviewDiff` (added/removed/changed
nodes and edges, each edge-change row carrying explicit
provenance/confidence/resolution deltas) for any of three comparison
kinds — snapshot↔snapshot, working-tree-vs-active, staged-vs-active — and
the viz app renders it as a raw diff list plus non-moving on-map badge
overlay on the frozen layout, with omission accounting for diffs too large
to return in one page.

## Minimum read neighborhood

- This card, the current checkpoint, and direct predecessor contract exports.
- `packages/store/src/diff.ts:21-104`
- `packages/mcp/src/service.ts:66-355`
- `@tadori/store`
- `packages/mcp/src/contracts.ts:49-88`
- `packages/server`
- `blueprints/07-01-server-graph-api.md`
- `packages/store/src/diff.ts`
- `packages/indexer/src/diffWorkingTree.ts`
- `packages/store/src/snapshots.ts`
- `packages/mcp/src/contracts.ts`
- `.js`

## Owned output neighborhood

- `packages/server/src/routes/reviewDiff.ts`
- `packages/server/src/reviewDiffAssembly.ts`
- `packages/indexer/src/captureStagedTree.ts`
- `packages/indexer/package.json`
- `packages/indexer/src/index.ts`
- `apps/viz/src/views/ReviewDiffView.tsx`
- `apps/viz/src/overlays/DiffBadgeOverlay.tsx`
- `/api/v1/layout`
- `apps/viz/src/api/reviewDiff.ts`
- `packages/server/test/reviewDiff.test.ts`
- `packages/indexer/test/captureStagedTree.test.ts`
- `blueprints/ARCHITECTURE.md`

## Execution slices

### Slice A

- Write `packages/indexer/test/captureStagedTree.test.ts` covering: a temp git repo with a staged add, a staged modify, a staged delete, and an unstaged-only change (must NOT appear). Add `simple-git` to `packages/indexer/package.json`. Implement `captureStagedTree`. Reason: staged diff has no existing store/indexer primitive; build and pin its contract before the route depends on it. Expected: new tests fail then pass; existing indexer suite (170+) stays green.
- Write `packages/server/test/reviewDiffAssembly.test.ts` for `diffNodesByEntityKey`, `rekeyEdgeDiffRows`, `paginateReviewDiff` against fixture 04's `before-graph.json`/`after-graph.json` (loaded via `@tadori/harness`'s `loadExpectedGraph`, not through the live indexer — these are pure functions over already-loaded graphs). Assert the raw output is loss-lessly comparable to `expected/raw-diff.json`'s `addedNodes`/`removedNodes`/`addedEdges`/`removedEdges`/ `changedEdgeMemberships` shape (field-by-field, not byte-identical JSON — the wire shape differs from the fixture oracle's shape by design, documented in §13). Implement the three functions. Expected: green.
- Write `packages/server/test/reviewDiff.test.ts`: route registration, the five HTTP-behavior rows in §10's table, snapshot↔snapshot happy path against a real indexed fixture-04 before/after pair inserted into a temp store DB. Implement `packages/server/src/routes/reviewDiff.ts` wiring the three comparison kinds. Expected: green; `pnpm typecheck` clean.
- Focused proof: run only tests that directly observe this slice’s contract edge.
- Preservation: checkpoint/commit when the slice is coherent; push the draft branch when usage risk is material.

### Slice B

- Build `apps/viz/src/api/reviewDiff.ts` typed client + cursor state.
- Build `apps/viz/src/views/ReviewDiffView.tsx` (list, reusing 08-06 row components) and `apps/viz/src/overlays/DiffBadgeOverlay.tsx` (reads existing layout positions; renders badges; no recomputation). Manual verification: load the view against fixture 04's before/after snapshots served through a real running server, confirm badges land on the already-fixed layout coordinates and the list matches `raw-diff.json` semantically.
- Full validation gate (§15).
- Focused proof: run only tests that directly observe this slice’s contract edge.
- Preservation: checkpoint/commit when the slice is coherent; push the draft branch when usage risk is material.

## Completion proof cut

- [ ] `GET /api/v1/review/diff` implements all three comparison kinds and the five response rows in §10's HTTP-behavior table.
- [ ] `ReviewDiff.edges` for fixture 04 before→after semantically matches every node/edge in `expected/raw-diff.json` (add/remove/changed-edge sets equal by entity key).
- [ ] `coalesce=coalesced` returns 501, never a silent raw substitution.
- [ ] Pagination returns explicit `nodesOmitted`/`edgesOmitted` counts and a working `nextCursor`; a paginated multi-page fetch reconstructs the full diff with zero duplicate or missing rows.
- [ ] The raw diff UI badge overlay renders at coordinates read from `/api/v1/layout` with zero layout recomputation calls (verified: no graphology layout invocation in the viz bundle's diff view code path).
- [ ] `captureStagedTree` excludes unstaged-only changes and includes staged deletes as absent from the head capture.
- [ ] Full existing suite (170+ tests) and 5/5 fixtures stay green.
- [ ] `pnpm typecheck`, `pnpm lint` clean on all new files.
- [ ] Independent validator reports no blocker/high finding.
- [ ] One risk-selected local completion gate passes.
- [ ] Required CI passes before merge.

## Dossier sections to open on demand

- Architecture/contract section only for a disputed public or persisted edge.
- Failure/security/accessibility sections only when the diff touches those neighborhoods.
- Review history only for an unresolved blocker/high.

## Stop boundaries

- Stop for a frozen-contract contradiction, absent predecessor contract, schema/migration data-loss risk, or inability to restore focused proofs without weakening an invariant.
- Do not stop for stale line numbers, historical “does not exist” prose, or a necessary one-hop wiring file.

## Delivery

- Completed node: status delta → commit/push → PR → CI → merge → immediately select the next frontier node.
- Usage cutoff: preserve a coherent slice in a commit and draft PR; never leave the only copy uncommitted.
