# Execution card 00-01A: allowJs scanner contract & regression

> This is the executable graph node. Open the long dossier only for a linked contract detail.

## Node

- State: `validated`
- Phase / risk / complexity: `0` / `low` / `S`
- Predecessors: none
- Successors: `00-01`
- Dossier: `blueprints/00-01A-allowjs-scanner-contract.md`
- Dossier size: approximately `4,712` tokens
- Full local gate budget: `1`
- Independent validation budget: `1`

## Graph rewrite

`scanRepository` classifies `.js/.jsx/.mjs/.cjs` files as indexed **only**
when the repository's effective TypeScript configuration enables JavaScript
(`allowJs` or `checkJs`); otherwise they are support files (still captured
and hashed). `pnpm tadori diff .` completes successfully on Tadori's own
repository.

## Minimum read neighborhood

- This card, the current checkpoint, and direct predecessor contract exports.
- `packages/indexer/src/scan.ts`
- `packages/indexer/src/project.ts`
- `packages/indexer/src/indexRepository.ts:32-50`
- `eslint.config.js`
- `tsconfig.base.json`
- `tsconfig.json`
- `mcp/service.ts:264`
- `mcp/service.ts:265`
- `.js`
- `packages/harness/src/compare.ts:205-215`
- `packages/indexer/src/indexRepository.ts`
- `packages/indexer/test/`
- `mcp/service.ts`

## Owned output neighborhood

- `packages/indexer/src/scan.ts`
- `./project.js`
- `packages/indexer/src/project.ts`
- `packages/indexer/test/scan-allowjs.test.ts`
- `IMPLEMENTATION_STATUS.md`
- `blueprints/INDEX.md`
- `BACKLOG.md`

## Execution slices

### Slice A

- `packages/indexer/test/scan-allowjs.test.ts`: write failing tests (a)–(f) from §13 (temp dirs via the repo's existing test tmp helpers; each test writes its own tsconfig/files). Run `pnpm test` — new tests fail with the current crash/classification; 170 existing tests still pass.
- `packages/indexer/src/project.ts`: add `resolveRootCompilerOptions` (live-disk wrapper over `findTsconfig` + `parseTsconfig`; returns `{ ...DEFAULT_OPTIONS }` when `findTsconfig` yields null). Reason: one shared tsconfig interpretation. Typecheck passes.
- `packages/indexer/src/scan.ts`: compute `allowJs` in `scanRepository` via the new export; thread it into `classifyFile`; JS branch returns `indexed: allowJs`. Reason: root-cause gate. Tests (a)–(f) go green.
- Focused proof: run only tests that directly observe this slice’s contract edge.
- Preservation: checkpoint/commit when the slice is coherent; push the draft branch when usage risk is material.

### Slice B

- Run the full gate (§15) including `pnpm tadori diff .` on the Tadori repo (must exit 0 and print a diff summary) and `echo "" | pnpm mcp:stdio --db .tadori/tadori.sqlite --repo .` (starts, clean EOF shutdown, exit 0) — completing 00-01's outstanding README command verification for these two commands.
- Update `IMPLEMENTATION_STATUS.md` (defect record + validation evidence), `blueprints/INDEX.md` (00-01A → built/validated; 00-01 unblocked), `BACKLOG.md`. Commit: `fix(indexer): gate JavaScript scan classification on effective allowJs`.
- Focused proof: run only tests that directly observe this slice’s contract edge.
- Preservation: checkpoint/commit when the slice is coherent; push the draft branch when usage risk is material.

## Completion proof cut

- [ ] `pnpm tadori diff .` exits 0 at the Tadori repo root and prints a diff summary.
- [ ] Tests (a)–(g) plus (b2) pass; full suite = exactly 170 existing + the new tests in `scan-allowjs.test.ts` (8 per §13), 0 failures. Any test beyond the §13 matrix is permitted only if named and justified in the builder's final report.
- [ ] 5/5 fixtures PASS with zero missing/unexpected/mismatched entities and zero dangling endpoints / FK rows.
- [ ] `echo "" | pnpm mcp:stdio --db .tadori/tadori.sqlite --repo .` exits 0 after clean EOF shutdown.
- [ ] `pnpm benchmark:incremental` gates still pass (single-file p95 < 2000 ms on the benchmark corpus).
- [ ] No public API signature changed except the additive `resolveRootCompilerOptions` export.
- [ ] `IMPLEMENTATION_STATUS.md` records the defect + fix with commit SHA.
- [ ] Independent validator reports no blocker/high finding.
- [ ] One risk-selected local completion gate passes.
- [ ] Required CI passes before merge.

## Dossier sections to open on demand

- Architecture/contract section only for a disputed public or persisted edge.
- Failure/security/accessibility sections only when the diff touches those neighborhoods.
- Review history only for an unresolved blocker/high.

## Stop boundaries

- Stop for a frozen-contract contradiction, absent predecessor contract, schema/migration data-loss risk, or inability to restore focused proofs without weakening an invariant.
- Do not stop for stale line numbers, historical “does not exist” prose, or a necessary one-hop wiring file.

## Delivery

- Completed node: status delta → commit/push → PR → CI → merge → immediately select the next frontier node.
- Usage cutoff: preserve a coherent slice in a commit and draft PR; never leave the only copy uncommitted.
