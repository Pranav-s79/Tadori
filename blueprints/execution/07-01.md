# Execution card 07-01: `packages/server` graph API

> This is the executable graph node. Open the long dossier only for a linked contract detail.

## Node

- State: `validated`
- Phase / risk / complexity: `7` / `high` / `M`
- Predecessors: `00-01`
- Successors: `07-02`, `08-01`, `08-08`
- Dossier: `blueprints/07-01-server-graph-api.md`
- Dossier size: approximately `13,414` tokens
- Full local gate budget: `1`
- Independent validation budget: `1`

## Graph rewrite

`packages/server` exists as a new pnpm workspace member exposing a
read-mostly Fastify HTTP+WS API on `127.0.0.1` that serves the current
snapshot's nodes/edges/evidence/search/source over the existing
`GraphService` seam, plus exactly one write endpoint
(`POST /api/v1/observations`), with zero new MCP tools.

## Minimum read neighborhood

- This card, the current checkpoint, and direct predecessor contract exports.
- `blueprints/research/EVIDENCE-BASELINE.md`
- `packages/server`
- `ls packages/`
- `package.json`
- `@tadori/mcp`
- `@modelcontextprotocol/sdk`
- `packages/mcp/src/service.ts:66-355`
- `@tadori/core`
- `@tadori/indexer`
- `@tadori/store`
- `/`
- `packages/mcp/src/concurrentRefresh.ts:52`
- `/api/v1/refresh`
- `packages/mcp/src/events.ts:65-360`
- `EVIDENCE-BASELINE.md`
- `packages/mcp/src/contracts.ts:49-88`

## Owned output neighborhood

- `packages/server/package.json`
- `name: "@tadori/server"`
- `./src/index.ts`
- `@fastify/websocket: "^11.x"`
- `@tadori/mcp: workspace:*`
- `packages/server/src/index.ts`
- `export * from "./app.js"`
- `export * from "./types.js"`
- `packages/server/src/types.ts`
- `packages/server/src/app.ts`
- `@fastify/websocket`
- `packages/server/src/graphState.ts`
- `packages/server/src/routes/snapshots.ts`
- `/snapshot`
- `/snapshots`
- `/snapshots/:id/pin`
- `packages/server/src/routes/graph.ts`
- `/nodes`
- `/edges`
- `/nodes/:entityKey`
- `/nodes/:entityKey/evidence`
- `packages/server/src/routes/source.ts`
- `/source`
- `packages/server/src/routes/search.ts`
- `/search`
- `packages/server/src/routes/path.ts`
- `/path`
- `packages/server/src/routes/derived.ts`
- `/tests`
- `/routes`

## Execution slices

### Slice A

- `packages/server/package.json` + workspace/tsconfig wiring (§9). Run `pnpm install`. Expected: `pnpm -w list --depth -1` shows `@tadori/server`.
- `packages/server/test/fixtures/buildTestDb.ts` — build the temp-file DB helper. Test added: a trivial smoke test asserting the helper returns a DB with one active snapshot. Expected: green.
- `packages/server/src/types.ts` — all DTOs. `pnpm typecheck` clean (no route logic yet, just types).
- Focused proof: run only tests that directly observe this slice’s contract edge.
- Preservation: checkpoint/commit when the slice is coherent; push the draft branch when usage risk is material.

### Slice B

- `packages/server/src/errors.ts` + `packages/server/src/graphState.ts` — `GraphState` wrapping `GraphService.open` + `ConcurrentRefreshController` + `EventLog`, per §8 decisions. Test added (`packages/server/test/graphState.test.ts` if the builder judges it needed, or folded into `snapshots.test.ts`): asserts `.current().snapshot` matches the fixture DB's active snapshot; asserts `.close()` stops the refresh worker (no open handle leak — assert via `db.close()` not throwing afterward).
- `packages/server/src/app.ts` skeleton (Fastify instance, no routes yet, `127.0.0.1`-only listen option threaded through options but not called). Test added: `localhost-bind.test.ts` — asserts `fastify.listen({host: "0.0.0.0", ...})` is never called anywhere in source (grep-based static assertion is acceptable here since binding is a config value, not runtime-observable without an actual listen) **and** an integration check: `app.listen({port: 0})` succeeds, `app.server.address().address` is `"127.0.0.1"`.
- Routes #1-#3 (`snapshots.ts`). Tests in `snapshots.test.ts`: `/snapshot` returns the fixture DB's active snapshot context; `/snapshots` lists it; `/snapshots/:id/pin` toggles `pinned` and returns the updated row; pinning an unknown id returns 404 `unknown_snapshot`.
- Focused proof: run only tests that directly observe this slice’s contract edge.
- Preservation: checkpoint/commit when the slice is coherent; push the draft branch when usage risk is material.

### Slice C

- Routes #4-#7 (`graph.ts`). Tests in `graph.test.ts`: `/nodes?level=package` returns only package-kind nodes from the fixture graph with correct pagination (`limit=1` returns exactly 1 item + non-null `nextCursor`); `/edges?relation=imports` filters correctly; `/nodes/:entityKey` for a known fixture entity key returns its out/in edges and `fanIn` matching `GraphService.fanIn`; unknown key returns 404 `unknown_entity`; `/nodes/:entityKey/evidence` returns the node's `evidence` array.
- Route #8 (`source.ts`). Tests in `source.test.ts`: reading a fixture file inside the repo root returns its body; `file=../outside.ts` (path-escape attempt) returns 403 `outside_repository`; a file not in the snapshot's file set returns 404 `not_in_snapshot`.
- Route #9 (`search.ts`). Tests in `search.test.ts`: a known fixture symbol name returns a match with `total >= 1`; empty `q` returns 400 `empty_query`.
- Focused proof: run only tests that directly observe this slice’s contract edge.
- Preservation: checkpoint/commit when the slice is coherent; push the draft branch when usage risk is material.

### Slice D

- Route #10 (`path.ts`). Test: a known two-hop fixture path returns `found: true` with intermediate nodes; an unreachable pair returns `found: false`.
- Routes #11-#13, #16-#18 (`derived.ts`). Tests: each stub route returns its documented honest "not yet available"/"not observed inspected" body with a 200 (not 500) — these are placeholders, not failures.
- Route #14 (`refresh.ts`). Tests in `refresh.test.ts`: reflects `ConcurrentRefreshController.state()` verbatim (start a real controller against the file-backed fixture DB — this is the one route test that needs the actual worker thread, per §4 gotcha).
- Focused proof: run only tests that directly observe this slice’s contract edge.
- Preservation: checkpoint/commit when the slice is coherent; push the draft branch when usage risk is material.

### Slice E

- Route #15 (`layout.ts`). Test: no `layout_positions` row for the fixture repo → 404 `layout_not_materialized`.
- Route #19 (`review.ts`). Test in a new or existing file: diff between the fixture DB's single snapshot and itself returns empty `nodesAdded`/ `nodesRemoved`/`edges` with `presentation: "raw"`.
- Route #20 (`observations.ts`). Tests in `observations.test.ts`: a well-formed `plan_mentioned` event with no targets is accepted (`accepted: 1`); an event referencing an unknown node `ref` is rejected with a per-item reason and `accepted: 0` for that item (batch partial acceptance, not all-or-nothing — see §17); a malformed body (missing `type`) returns 400 `bad_schema` for the whole request.
- Focused proof: run only tests that directly observe this slice’s contract edge.
- Preservation: checkpoint/commit when the slice is coherent; push the draft branch when usage risk is material.

### Slice F

- `ws.ts` + wiring into `app.ts`. Tests in `ws.test.ts`: connect, subscribe to `["refresh"]`, trigger a refresh-state change on the underlying controller (or a test double), assert a `refresh_pending`/ `refresh_settled` frame arrives; assert no frame arrives for an unsubscribed channel.
- `performance.test.ts` — build (or reuse a cached) larger fixture DB approximating the 250k-LOC benchmark corpus (reuse `scripts/benchmark-incremental.mts`'s corpus generator if practical, or document why a smaller proxy corpus with a scaled budget is used instead — see §16). Assert `/nodes?level=package` p95 < 200 ms over N repeated `fastify.inject()` calls.
- Full validation pass (§15). Commit.
- Focused proof: run only tests that directly observe this slice’s contract edge.
- Preservation: checkpoint/commit when the slice is coherent; push the draft branch when usage risk is material.

## Completion proof cut

- [ ] `packages/server` builds: `pnpm typecheck` exits 0 including the new package.
- [ ] `pnpm -w list --depth -1` includes `@tadori/server`.
- [ ] All 20 routes in §10's table exist and return the documented shape for at least one passing case each (verified by the named tests in §11).
- [ ] `fastify.listen({host: "127.0.0.1", port: 0})` succeeds and `app.server.address().address === "127.0.0.1"`; no source line passes `"0.0.0.0"` as a listen host (`grep -r "0.0.0.0" packages/server/src` returns zero matches).
- [ ] `/nodes` and `/edges` pagination: requesting `limit=1` against a fixture with ≥ 2 matching rows returns exactly 1 item and a non-null `nextCursor`; following the cursor returns the next item.
- [ ] `POST /observations` with one well-formed and one malformed event in the same batch returns `accepted: 1` and one entry in `rejected` (partial-batch acceptance, not all-or-nothing failure).
- [ ] `POST /api/v1/snapshots/:id/pin` on a nonexistent id returns HTTP 404 with `code: "unknown_snapshot"`.
- [ ] `/source?file=../outside.ts` (or equivalent escape attempt) returns HTTP 403 with `code: "outside_repository"`.
- [ ] WS: subscribing to `["refresh"]` and triggering a refresh-state change on the bound controller produces exactly one `refresh_pending` or `refresh_settled` frame; no frame for unsubscribed channels.
- [ ] `/nodes?level=package` p95 latency < 200 ms across ≥ 20 repeated `fastify.inject()` calls against a DB built from the 250k-LOC benchmark corpus (or the documented scaled proxy — see §16).
- [ ] No import of `@modelcontextprotocol/sdk`, `StdioServerTransport`, `createTadoriMcpServer`, or `TadoriTools` anywhere under `packages/server/src` (`grep -r "modelcontextprotocol\|StdioServerTransport\|createTadoriMcpServer\|TadoriTools" packages/server/src` returns zero matches).
- [ ] Full existing suite stays green: `pnpm test` reports the pre-existing count plus this blueprint's new tests, zero failures.
- [ ] 5/5 golden fixtures still PASS (`pnpm fixtures:validate`, `pnpm fixtures:index`) — this blueprint touches no fixture/indexer code, so this is a regression guard, not new coverage.
- [ ] An observation POSTed during the narrow post-rotation window (snapshot replaced, replacement EventLog not yet constructed — simulated via test double) returns 409 no_active_task and is not recorded.
- [ ] Independent validator reports no blocker/high finding.
- [ ] One risk-selected local completion gate passes.
- [ ] Required CI passes before merge.

## Dossier sections to open on demand

- Architecture/contract section only for a disputed public or persisted edge.
- Failure/security/accessibility sections only when the diff touches those neighborhoods.
- Review history only for an unresolved blocker/high.

## Stop boundaries

- Stop for a frozen-contract contradiction, absent predecessor contract, schema/migration data-loss risk, or inability to restore focused proofs without weakening an invariant.
- Do not stop for stale line numbers, historical “does not exist” prose, or a necessary one-hop wiring file.

## Delivery

- Completed node: status delta → commit/push → PR → CI → merge → immediately select the next frontier node.
- Usage cutoff: preserve a coherent slice in a commit and draft PR; never leave the only copy uncommitted.
