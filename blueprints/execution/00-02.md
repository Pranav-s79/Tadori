# Execution card 00-02: CI pipeline (Linux+Windows)

> This is the executable graph node. Open the long dossier only for a linked contract detail.

## Node

- State: `validated`
- Phase / risk / complexity: `0` / `low` / `S`
- Predecessors: `00-01`
- Successors: `11-01`, `12-03`
- Dossier: `blueprints/00-02-ci-pipeline.md`
- Dossier size: approximately `3,579` tokens
- Full local gate budget: `1`
- Independent validation budget: `1`

## Graph rewrite

Every push and pull request to `main` and `Sprint*` branches runs the full
frozen local gate on `ubuntu-latest` and `windows-latest`, and both jobs
pass.

## Minimum read neighborhood

- This card, the current checkpoint, and direct predecessor contract exports.
- `.github/`
- `package.json`
- `.github/workflows/ci.yml`
- `IMPLEMENTATION_STATUS.md`
- `blueprints/research/EVIDENCE-BASELINE.md`
- `actions/setup-node`

## Owned output neighborhood

- `.github/workflows/ci.yml`
- `README.md`
- `![CI](https://github.com/Pranav-s79/Tadori/actions/workflows/ci.yml/badge.svg)`
- `IMPLEMENTATION_STATUS.md`

## Execution slices

### Slice A

- Create `.github/workflows/ci.yml` per §10; validate YAML locally (`node -e` YAML parse or actionlint if available). Expected: file parses.
- Add README badge line + IMPLEMENTATION_STATUS subsection stub.
- Commit `ci: add Linux+Windows full-gate workflow`; push the current sprint branch only. Authorization: `blueprints/ASSUMPTIONS.md` A-001 and `BACKLOG.md` "Decisions locked 2026-07-15" ("Push authorized once for existing sprint branches + resulting `main`; no tags/releases") as re-scoped by the BACKLOG 2026-07-17 entry (sprint branches only; `main` via owner PRs; never force-push).
- Focused proof: run only tests that directly observe this slice’s contract edge.
- Preservation: checkpoint/commit when the slice is coherent; push the draft branch when usage risk is material.

### Slice B

- Observe both matrix jobs on GitHub Actions; iterate on runner-specific failures (each fix its own commit; never weaken a gate to pass — a gate that fails on Linux is a real finding, report it).
- Record first green run URLs/IDs per OS in IMPLEMENTATION_STATUS.md; flip INDEX/BACKLOG statuses.
- Focused proof: run only tests that directly observe this slice’s contract edge.
- Preservation: checkpoint/commit when the slice is coherent; push the draft branch when usage risk is material.

## Completion proof cut

- [ ] Both matrix jobs green on the same commit (run URLs recorded).
- [ ] The vitest summary lines (`Tests NNN passed`) from both CI OS logs and the local run of the same commit are quoted verbatim in the builder report and show equal counts.
- [ ] `fixtures:index` prints 5/5 PASS in both OS logs.
- [ ] A deliberately broken scratch commit fails CI (evidence: run URL), then is reverted.
- [ ] No secrets referenced; no publish/tag steps exist in the workflow.
- [ ] Badge renders in README on the branch.
- [ ] Independent validator reports no blocker/high finding.
- [ ] One risk-selected local completion gate passes.
- [ ] Required CI passes before merge.

## Dossier sections to open on demand

- Architecture/contract section only for a disputed public or persisted edge.
- Failure/security/accessibility sections only when the diff touches those neighborhoods.
- Review history only for an unresolved blocker/high.

## Stop boundaries

- Stop for a frozen-contract contradiction, absent predecessor contract, schema/migration data-loss risk, or inability to restore focused proofs without weakening an invariant.
- Do not stop for stale line numbers, historical “does not exist” prose, or a necessary one-hop wiring file.

## Delivery

- Completed node: status delta → commit/push → PR → CI → merge → immediately select the next frontier node.
- Usage cutoff: preserve a coherent slice in a commit and draft PR; never leave the only copy uncommitted.
