export function findSession(id: string): { id: string; user: string } {
  return { id, user: "fixture-user" };
}

export function insertSession(id: string): { id: string; user: string } {
  return { id, user: "fixture-user" };
}
